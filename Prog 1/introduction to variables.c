#include <stdio.h>
#include <stdlib.h>
	int main(){
		char characterName[] = "Ron";
		int characterAge = 19;
		printf("There once was a boy named %s\n", characterName);
		printf("He was %d years old.\n", characterAge);
		printf("He really liked the name %s.\n", characterName);
		printf("But did not like being %d.", characterAge);
		return 0;	
	}

